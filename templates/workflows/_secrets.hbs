{{#if on.workflow_call.secrets}}
### Secrets

{{#if inheritedSecrets}}
> This workflow can use [inherited secrets](https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idsecretsinherit).
These secrets should be present in either organization or repository action secrets, the workflow will fail if `required` secrets are not available

|  Secret  |  Required at run  | Inherited Key | Description |
| :------: | :---------------: | :-----------: | ----------- |
{{#EachSorted on.workflow_call.secrets}}
| <pre>{{@key}}</pre> | {{#if this.required}}☑️{{else if this.maybeRequired}}🤔{{else}}-{{/if}} | {{#if this.inheritedKey}}`{{this.inheritedKey}}`{{/if}} | {{ReplaceNewLines this.description}} |
{{/EachSorted}}
{{else}}

|  Secret  |  Required  | Description |
| :------: | :--------: | ----------- |
{{#EachSorted on.workflow_call.secrets}}
| <pre>{{@key}}</pre> | {{#if this.required}}☑️{{else if this.maybeRequired}}🤔{{else}}-{{/if}}  | {{ReplaceNewLines this.description}} |
{{/EachSorted}}
{{/if}}
{{/if}}

